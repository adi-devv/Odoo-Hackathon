import 'package:flutter/material.dart';

class PostQuestionPage extends StatelessWidget {
  const PostQuestionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(),
    );
  }
}
